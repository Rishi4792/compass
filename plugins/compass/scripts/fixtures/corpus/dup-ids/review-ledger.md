# Review ledger — dup-ids

| Issue ID | Review | Round | Affected area | Failure mode | Impacted invariant | Severity | Root cause | Fix | Validation | Owner stream | Status |
|---|---|---|---|---|---|---|---|---|---|---|---|
| D-1 | R1 | 1 | Dedupe | clause 1 of a semicolon separated failure mode that runs well past the field budget; clause 2 of a semicolon separated failure mode that runs well past the field budget; clause 3 of a semicolon separated failure mode that runs well past the field budget; clause 4 of a semicolon separated failure mode that runs well past the field budget; clause 5 of a semicolon separated failure mode that runs well past the field budget; clause 6 of a semicolon separated failure mode that runs well past the field budget; clause 7 of a semicolon separated failure mode that runs well past the field budget; clause 8 of a semicolon separated failure mode that runs well past the field budget; clause 9 of a semicolon separated failure mode that runs well past the field budget | INV-DUP | MAJOR | root a | fix a | `true` | stream | OPEN |
| D-1 | R1 | 2 | Dedupe | The field was shortened at a sentence boundary and the remainder discarded. That remainder held the reason the finding was rated critical, so the page states a severity it no longer justifies. Nothing on the page indicates that anything was removed. | INV-DUP | CRITICAL | root b | fix b | `true` | stream | OPEN |
| D-2 | R1 | 1 | Rendering | a failure mode with no sentence boundary anywhere in its text so it falls through to the hard cut and loses its tail with nothing to mark the loss | INV-DUP | MAJOR | root c | fix c | `true` | stream | FIXED |
| D-3 | R1 | 1 | Sentence cut | The kept half of this field ends on a full stop and reads as a finished thought, which is exactly why the loss is invisible to a reader and to the cut check. The discarded half held the reason, the measurement and the command that reproduces it, none of which appears anywhere on the rendered page. | INV-DUP | MAJOR | root d | fix d | `true` | stream | OPEN |
| D-4 | R1 | 1 | Sentence cut wide | This first sentence is deliberately long enough to clear the half-of-budget floor that the wider field budgets impose, so that the sentence-boundary branch is the one that runs rather than the hard cut that follows it. Everything after this point is destroyed, and the page says only that the text continues, not how much of it is gone or where a reader could go to find it. A second discarded sentence, so the remainder is unambiguous. | INV-DUP | MAJOR | root e | fix e | `true` | stream | FIXED |
