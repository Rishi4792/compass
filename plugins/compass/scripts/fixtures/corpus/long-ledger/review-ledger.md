# Review ledger — long-ledger

| Issue ID | Review | Round | Affected area | Failure mode | Impacted invariant | Severity | Root cause | Fix | Validation | Owner stream | Status |
|---|---|---|---|---|---|---|---|---|---|---|---|
| C-1 | R1 | 1 | Area 1 | a field with no sentence boundary anywhere in it which therefore falls through to the hard cut path and loses its tail without any full stop to make the loss look deliberate and this clause keeps running so the length is unambiguous | INV-LEDGER-CUT | MAJOR | root 1 | fix 1 | `true` | stream | OPEN |
| C-2 | R1 | 1 | Area 2 | part 1 of a semicolon separated list that runs past the field budget; part 2 of a semicolon separated list that runs past the field budget; part 3 of a semicolon separated list that runs past the field budget; part 4 of a semicolon separated list that runs past the field budget; part 5 of a semicolon separated list that runs past the field budget; part 6 of a semicolon separated list that runs past the field budget; part 7 of a semicolon separated list that runs past the field budget; part 8 of a semicolon separated list that runs past the field budget | INV-LEDGER-CUT | MAJOR | root 2 | fix 2 | `true` | stream | OPEN |
| C-3 | R1 | 1 | Area 3 | The renderer shortened this field at a sentence boundary and threw the remainder away. That remainder is the half that says what success looks like, and because the kept text ends in a full stop the cut check cannot see it either. A reader is left with a confident fragment and no way to reach the rest of the argument. | INV-LEDGER-CUT | MAJOR | root 3 | fix 3 | `true` | stream | OPEN |
| C-4 | R1 | 1 | Area 4 | a field with no sentence boundary anywhere in it which therefore falls through to the hard cut path and loses its tail without any full stop to make the loss look deliberate and this clause keeps running so the length is unambiguous | INV-LEDGER-CUT | MAJOR | root 4 | fix 4 | `true` | stream | OPEN |
| C-5 | R1 | 1 | Area 5 | part 1 of a semicolon separated list that runs past the field budget; part 2 of a semicolon separated list that runs past the field budget; part 3 of a semicolon separated list that runs past the field budget; part 4 of a semicolon separated list that runs past the field budget; part 5 of a semicolon separated list that runs past the field budget; part 6 of a semicolon separated list that runs past the field budget; part 7 of a semicolon separated list that runs past the field budget; part 8 of a semicolon separated list that runs past the field budget | INV-LEDGER-CUT | MAJOR | root 5 | fix 5 | `true` | stream | OPEN |
| C-6 | R1 | 1 | Area 6 | The renderer shortened this field at a sentence boundary and threw the remainder away. That remainder is the half that says what success looks like, and because the kept text ends in a full stop the cut check cannot see it either. A reader is left with a confident fragment and no way to reach the rest of the argument. | INV-LEDGER-CUT | MAJOR | root 6 | fix 6 | `true` | stream | OPEN |
| C-7 | R1 | 1 | Area 7 | a field with no sentence boundary anywhere in it which therefore falls through to the hard cut path and loses its tail without any full stop to make the loss look deliberate and this clause keeps running so the length is unambiguous | INV-LEDGER-CUT | MAJOR | root 7 | fix 7 | `true` | stream | FIXED |
| C-8 | R1 | 1 | Area 8 | part 1 of a semicolon separated list that runs past the field budget; part 2 of a semicolon separated list that runs past the field budget; part 3 of a semicolon separated list that runs past the field budget; part 4 of a semicolon separated list that runs past the field budget; part 5 of a semicolon separated list that runs past the field budget; part 6 of a semicolon separated list that runs past the field budget; part 7 of a semicolon separated list that runs past the field budget; part 8 of a semicolon separated list that runs past the field budget | INV-LEDGER-CUT | MAJOR | root 8 | fix 8 | `true` | stream | FIXED |
| C-9 | R1 | 1 | Area 9 | The renderer shortened this field at a sentence boundary and threw the remainder away. That remainder is the half that says what success looks like, and because the kept text ends in a full stop the cut check cannot see it either. A reader is left with a confident fragment and no way to reach the rest of the argument. | INV-LEDGER-CUT | MAJOR | root 9 | fix 9 | `true` | stream | FIXED |
| C-10 | R1 | 1 | Area 10 | a field with no sentence boundary anywhere in it which therefore falls through to the hard cut path and loses its tail without any full stop to make the loss look deliberate and this clause keeps running so the length is unambiguous | INV-LEDGER-CUT | MAJOR | root 10 | fix 10 | `true` | stream | FIXED |
| C-11 | R1 | 1 | Area 11 | part 1 of a semicolon separated list that runs past the field budget; part 2 of a semicolon separated list that runs past the field budget; part 3 of a semicolon separated list that runs past the field budget; part 4 of a semicolon separated list that runs past the field budget; part 5 of a semicolon separated list that runs past the field budget; part 6 of a semicolon separated list that runs past the field budget; part 7 of a semicolon separated list that runs past the field budget; part 8 of a semicolon separated list that runs past the field budget | INV-LEDGER-CUT | MAJOR | root 11 | fix 11 | `true` | stream | FIXED |
| C-12 | R1 | 1 | Area 12 | The renderer shortened this field at a sentence boundary and threw the remainder away. That remainder is the half that says what success looks like, and because the kept text ends in a full stop the cut check cannot see it either. A reader is left with a confident fragment and no way to reach the rest of the argument. | INV-LEDGER-CUT | MAJOR | root 12 | fix 12 | `true` | stream | FIXED |
| C-13 | R1 | 1 | Area 13 | a field with no sentence boundary anywhere in it which therefore falls through to the hard cut path and loses its tail without any full stop to make the loss look deliberate and this clause keeps running so the length is unambiguous | INV-LEDGER-CUT | MAJOR | root 13 | fix 13 | `true` | stream | FIXED |
| C-14 | R1 | 1 | Area 14 | part 1 of a semicolon separated list that runs past the field budget; part 2 of a semicolon separated list that runs past the field budget; part 3 of a semicolon separated list that runs past the field budget; part 4 of a semicolon separated list that runs past the field budget; part 5 of a semicolon separated list that runs past the field budget; part 6 of a semicolon separated list that runs past the field budget; part 7 of a semicolon separated list that runs past the field budget; part 8 of a semicolon separated list that runs past the field budget | INV-LEDGER-CUT | MAJOR | root 14 | fix 14 | `true` | stream | FIXED |
| C-15 | R1 | 1 | Area 15 | The renderer shortened this field at a sentence boundary and threw the remainder away. That remainder is the half that says what success looks like, and because the kept text ends in a full stop the cut check cannot see it either. A reader is left with a confident fragment and no way to reach the rest of the argument. | INV-LEDGER-CUT | MAJOR | root 15 | fix 15 | `true` | stream | FIXED |
| C-16 | R1 | 1 | Area 16 | a field with no sentence boundary anywhere in it which therefore falls through to the hard cut path and loses its tail without any full stop to make the loss look deliberate and this clause keeps running so the length is unambiguous | INV-LEDGER-CUT | MAJOR | root 16 | fix 16 | `true` | stream | FIXED |
| C-17 | R1 | 1 | Area 17 | part 1 of a semicolon separated list that runs past the field budget; part 2 of a semicolon separated list that runs past the field budget; part 3 of a semicolon separated list that runs past the field budget; part 4 of a semicolon separated list that runs past the field budget; part 5 of a semicolon separated list that runs past the field budget; part 6 of a semicolon separated list that runs past the field budget; part 7 of a semicolon separated list that runs past the field budget; part 8 of a semicolon separated list that runs past the field budget | INV-LEDGER-CUT | MAJOR | root 17 | fix 17 | `true` | stream | FIXED |
| C-18 | R1 | 1 | Area 18 | The renderer shortened this field at a sentence boundary and threw the remainder away. That remainder is the half that says what success looks like, and because the kept text ends in a full stop the cut check cannot see it either. A reader is left with a confident fragment and no way to reach the rest of the argument. | INV-LEDGER-CUT | MAJOR | root 18 | fix 18 | `true` | stream | FIXED |
| C-19 | R1 | 1 | Area 19 | a field with no sentence boundary anywhere in it which therefore falls through to the hard cut path and loses its tail without any full stop to make the loss look deliberate and this clause keeps running so the length is unambiguous | INV-LEDGER-CUT | MAJOR | root 19 | fix 19 | `true` | stream | FIXED |
| C-20 | R1 | 1 | Area 20 | part 1 of a semicolon separated list that runs past the field budget; part 2 of a semicolon separated list that runs past the field budget; part 3 of a semicolon separated list that runs past the field budget; part 4 of a semicolon separated list that runs past the field budget; part 5 of a semicolon separated list that runs past the field budget; part 6 of a semicolon separated list that runs past the field budget; part 7 of a semicolon separated list that runs past the field budget; part 8 of a semicolon separated list that runs past the field budget | INV-LEDGER-CUT | MAJOR | root 20 | fix 20 | `true` | stream | FIXED |
| C-21 | R1 | 1 | Area 21 | The renderer shortened this field at a sentence boundary and threw the remainder away. That remainder is the half that says what success looks like, and because the kept text ends in a full stop the cut check cannot see it either. A reader is left with a confident fragment and no way to reach the rest of the argument. | INV-LEDGER-CUT | MAJOR | root 21 | fix 21 | `true` | stream | FIXED |
| C-22 | R1 | 1 | Area 22 | a field with no sentence boundary anywhere in it which therefore falls through to the hard cut path and loses its tail without any full stop to make the loss look deliberate and this clause keeps running so the length is unambiguous | INV-LEDGER-CUT | MAJOR | root 22 | fix 22 | `true` | stream | FIXED |
| C-23 | R1 | 1 | Area 23 | part 1 of a semicolon separated list that runs past the field budget; part 2 of a semicolon separated list that runs past the field budget; part 3 of a semicolon separated list that runs past the field budget; part 4 of a semicolon separated list that runs past the field budget; part 5 of a semicolon separated list that runs past the field budget; part 6 of a semicolon separated list that runs past the field budget; part 7 of a semicolon separated list that runs past the field budget; part 8 of a semicolon separated list that runs past the field budget | INV-LEDGER-CUT | MAJOR | root 23 | fix 23 | `true` | stream | FIXED |
| C-24 | R1 | 1 | Area 24 | The renderer shortened this field at a sentence boundary and threw the remainder away. That remainder is the half that says what success looks like, and because the kept text ends in a full stop the cut check cannot see it either. A reader is left with a confident fragment and no way to reach the rest of the argument. | INV-LEDGER-CUT | MAJOR | root 24 | fix 24 | `true` | stream | FIXED |
| C-25 | R1 | 1 | Area 25 | a field with no sentence boundary anywhere in it which therefore falls through to the hard cut path and loses its tail without any full stop to make the loss look deliberate and this clause keeps running so the length is unambiguous | INV-LEDGER-CUT | MAJOR | root 25 | fix 25 | `true` | stream | FIXED |
| C-26 | R1 | 1 | Area 26 | part 1 of a semicolon separated list that runs past the field budget; part 2 of a semicolon separated list that runs past the field budget; part 3 of a semicolon separated list that runs past the field budget; part 4 of a semicolon separated list that runs past the field budget; part 5 of a semicolon separated list that runs past the field budget; part 6 of a semicolon separated list that runs past the field budget; part 7 of a semicolon separated list that runs past the field budget; part 8 of a semicolon separated list that runs past the field budget | INV-LEDGER-CUT | MAJOR | root 26 | fix 26 | `true` | stream | FIXED |
| C-27 | R1 | 1 | Area 27 | The renderer shortened this field at a sentence boundary and threw the remainder away. That remainder is the half that says what success looks like, and because the kept text ends in a full stop the cut check cannot see it either. A reader is left with a confident fragment and no way to reach the rest of the argument. | INV-LEDGER-CUT | MAJOR | root 27 | fix 27 | `true` | stream | FIXED |
| C-28 | R1 | 1 | Area 28 | a field with no sentence boundary anywhere in it which therefore falls through to the hard cut path and loses its tail without any full stop to make the loss look deliberate and this clause keeps running so the length is unambiguous | INV-LEDGER-CUT | MAJOR | root 28 | fix 28 | `true` | stream | FIXED |
| C-29 | R1 | 1 | Area 29 | part 1 of a semicolon separated list that runs past the field budget; part 2 of a semicolon separated list that runs past the field budget; part 3 of a semicolon separated list that runs past the field budget; part 4 of a semicolon separated list that runs past the field budget; part 5 of a semicolon separated list that runs past the field budget; part 6 of a semicolon separated list that runs past the field budget; part 7 of a semicolon separated list that runs past the field budget; part 8 of a semicolon separated list that runs past the field budget | INV-LEDGER-CUT | MAJOR | root 29 | fix 29 | `true` | stream | FIXED |
| C-30 | R1 | 1 | Area 30 | The renderer shortened this field at a sentence boundary and threw the remainder away. That remainder is the half that says what success looks like, and because the kept text ends in a full stop the cut check cannot see it either. A reader is left with a confident fragment and no way to reach the rest of the argument. | INV-LEDGER-CUT | MAJOR | root 30 | fix 30 | `true` | stream | FIXED |
| C-31 | R1 | 1 | Area 31 | a field with no sentence boundary anywhere in it which therefore falls through to the hard cut path and loses its tail without any full stop to make the loss look deliberate and this clause keeps running so the length is unambiguous | INV-LEDGER-CUT | MAJOR | root 31 | fix 31 | `true` | stream | FIXED |
| C-32 | R1 | 1 | Area 32 | part 1 of a semicolon separated list that runs past the field budget; part 2 of a semicolon separated list that runs past the field budget; part 3 of a semicolon separated list that runs past the field budget; part 4 of a semicolon separated list that runs past the field budget; part 5 of a semicolon separated list that runs past the field budget; part 6 of a semicolon separated list that runs past the field budget; part 7 of a semicolon separated list that runs past the field budget; part 8 of a semicolon separated list that runs past the field budget | INV-LEDGER-CUT | MAJOR | root 32 | fix 32 | `true` | stream | FIXED |
| C-33 | R1 | 1 | Area 33 | The renderer shortened this field at a sentence boundary and threw the remainder away. That remainder is the half that says what success looks like, and because the kept text ends in a full stop the cut check cannot see it either. A reader is left with a confident fragment and no way to reach the rest of the argument. | INV-LEDGER-CUT | MAJOR | root 33 | fix 33 | `true` | stream | FIXED |
| C-34 | R1 | 1 | Area 34 | a field with no sentence boundary anywhere in it which therefore falls through to the hard cut path and loses its tail without any full stop to make the loss look deliberate and this clause keeps running so the length is unambiguous | INV-LEDGER-CUT | MAJOR | root 34 | fix 34 | `true` | stream | FIXED |
