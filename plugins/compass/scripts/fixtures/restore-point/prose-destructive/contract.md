# Fixture — restore-point prose-destructive (R3-C1 regression: house-style "value → gloss")
schema-touching: yes → we backfill the accounts table and drop the legacy column
destructive-backfill: yes → rewrite every row in payments
