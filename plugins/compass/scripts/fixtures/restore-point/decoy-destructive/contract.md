# Fixture — restore-point decoy-destructive (R3-R2-D1: leading-prose decoy must NOT steal the parse)
> This build runs a destructive-backfill: it rewrites the amount on every order row.
schema-touching: no
destructive-backfill: yes
