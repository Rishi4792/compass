# Contract — ctl-mid-unit-cut
compass-format: v0.30
Facets: library
pii: no

## Goal & scope
A CONTROL fixture whose text runs long on purpose so the page truncates it somewhere that is not the end of a sentence, giving the cut check a real defect to find rather than a synthetic marker.

## Non-goals
This fixture ships no behaviour. It exists so a check has a real page to measure.

