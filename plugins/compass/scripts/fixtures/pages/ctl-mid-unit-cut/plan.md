# Plan — ctl-mid-unit-cut

## The approach
One short paragraph, so the page has an approach to show.

## Steps
- [x] **S1 Render a step whose description keeps running on and on, comma after comma, never reaching a full stop before the card runs out of room, so the cut has to land in the middle of a clause, which is the exact shape a reader complains about, and which no punctuation rule can rescue because there is no punctuation there to land on, and it keeps going even further than that to be certain**
      **VERIFY:** a command that would run here.
- [ ] **S2 Measure it.**
      **VERIFY:** a command that would run here.
- [ ] **S3 Report it.**
      **VERIFY:** a command that would run here.
