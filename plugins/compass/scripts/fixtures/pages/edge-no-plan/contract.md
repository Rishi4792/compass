# Contract — edge-no-plan
compass-format: v0.30
Facets: library
pii: no

## Goal & scope
An EDGE fixture with no plan at all, so a page that asks about a plan must say there is none rather than print zero.

## Non-goals
This fixture ships no behaviour. It exists so a check has a real page to measure.

