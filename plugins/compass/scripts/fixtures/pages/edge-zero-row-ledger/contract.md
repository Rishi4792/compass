# Contract — edge-zero-row-ledger
compass-format: v0.30
Facets: library
pii: no

## Goal & scope
An EDGE fixture whose ledger has a header and no rows, so the page must not read that as no findings.

## Non-goals
This fixture ships no behaviour. It exists so a check has a real page to measure.

