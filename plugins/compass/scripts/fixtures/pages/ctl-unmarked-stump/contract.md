# Contract — ctl-unmarked-stump
compass-format: v0.30
Facets: library
pii: no

## Goal & scope
A CONTROL fixture whose opening sentence deliberately stops on a.

## Non-goals
This fixture ships no behaviour. It exists so the unmarked-cut check has a real defect to find
rather than a synthetic one: the line above ends on a dangling word with a full stop welded to it
and no control to click, which is the shape a marker-counting check cannot see.
