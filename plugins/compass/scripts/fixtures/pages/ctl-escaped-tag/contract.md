# Contract — ctl-escaped-tag
compass-format: v0.30
Facets: library
pii: no

## Goal & scope
A CONTROL fixture whose prose carries a raw <span data-prov="counted">34</span> outside a code element on purpose, so the leak check has the real defect to find rather than a correctly escaped one.

## Non-goals
This fixture ships no behaviour. It exists so a check has a real page to measure.

