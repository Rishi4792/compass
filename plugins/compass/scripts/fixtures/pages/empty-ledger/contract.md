# Contract — empty-ledger
compass-format: v0.30
Facets: library
pii: no

## Goal & scope
A fixture whose ledger exists and holds no rows, so the page must say it could not count rather than say zero.

## Non-goals
This fixture ships no behaviour. It exists so a check has a real page to measure.

