# Contract — wrapped-bullet

<!-- A FIXTURE, not a build. Its only job is one bullet wrapped across two lines. It deliberately
     does NOT declare compass-format, because claiming that format obliges a reader-copy block and
     copy-gate is right to refuse one without it — the canary caught exactly that when this fixture
     was first written. A fixture should exercise one rule, not accidentally opt into all of them. -->

Facets: library
schema-touching: no

## 1. Goal & scope

A fixture whose whole job is one wrapped bullet.

## Scope ladder

- **NOW:** the thing being built.
- **LATER:** something else.
- **NEVER:** a third thing.
