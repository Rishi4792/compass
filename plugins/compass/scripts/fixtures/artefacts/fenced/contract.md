# Contract — fenced · v1

facets: library
schema-touching: no

## Goal & scope
The real goal, which lives in a proper section and must be the one that renders.

## Design Spec
An ASCII mockup. Everything inside the fence is a DRAWING, not data:

```
── Compass · <slug> ────────────────────
  Goal: <goal from INDEX>
  mode: <Human-gated | Autonomous>
- [ ] a checkbox that is part of the drawing, not a plan step
```

## Acceptance & INVARIANTs
- **INV-EXAMPLE:** something asserted. → *assert:* a command.
