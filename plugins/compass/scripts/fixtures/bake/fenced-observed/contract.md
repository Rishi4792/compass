# Fixture — bake fenced-observed (RB-M6: a bake-observed example inside a code fence is not a real reading)
bake-window: 5m
bake-bound: err=0 lat=200 mem=512
```
bake-observed: dur=600 err=0 lat=0 mem=0
```
