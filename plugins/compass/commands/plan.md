---
description: Turn a locked CONTRACT into an industry-standard engineering plan (after scanning the live codebase). Namespaced entry to the compass:plan stage.
---

# /compass:plan

Namespaced entry point for the Compass **plan** stage. It delegates to the `compass:plan` skill, which owns the full stage logic, reads `contract.md` as the invariant, and ends with the canonical 4-button transition gate.

**Do this now:** invoke the **Skill** tool with `skill: compass:plan`. Do not duplicate, summarize, or re-implement the stage here — run the skill. The skill presents the gate at its transition; this wrapper adds none.
