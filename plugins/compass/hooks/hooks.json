{
  "hooks": {
    "PreCompact": [
      {
        "matcher": "*",
        "hooks": [
          {
            "type": "command",
            "command": "echo '{\"systemMessage\":\"Compass: if a build is active, update .claude/builds/<slug>/progress.md now (stage, status, first UNCHECKED step, open ledger items) and print the resume block for /compass:resume. Never leave a plan.md box checked whose verify did not finish.\"}'"
          }
        ]
      }
    ]
  }
}
